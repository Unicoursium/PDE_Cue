# `prepare_clock_font.py`

Purpose: prepares a regular-weight clock font.

The kiosk can use `assets/clock_font.ttf`. This helper generates `assets/clock_font_regular.ttf` at a normal weight when needed.

It is a utility script for improving clock rendering consistency on the kiosk display.

