# `Raspberry_pi_Tests/button_test.py`

Purpose: early two-button GPIO test.

Uses GPIO23 and GPIO24 with pull-up input. It prints output when buttons are pressed. This is an older standalone version of `buttontest.py`.

