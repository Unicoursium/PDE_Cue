# `Raspberry_pi_Tests/led_test.py`

Purpose: early LED service test.

This script sends colors to the LED service through `led_client.py`. It was originally written for a 40 LED layout, so use the current `Cue_Hub` scripts for the final 55 LED hub.

